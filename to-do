To Do:

set header to width 100%

revise units

set h4s to thick and thin border in a standout color

add opacity to a:hover to make it less opaque when hovered over

use ::marker to style bullet points

add navigation to center of blog by month, use inline-block boxes with a dark color
and light colored text with margins in between them
use columns

learn how to link to different pages of a blog in html

profile pic: in surround text use display: flow-root instead of br, limit br's

Preview code I'm having trouble with in a code box.
